.header
  %h1= link_to "LOLIDAY PLANNER", root_path, :id => "header-title"
  .header-links-position
    - if current_user
      %p= "Hi #{current_user.first_name} 👋"
      = link_to 'Logout', logout_path
    - else
      = link_to 'Signup', signup_path, :id => "signup"
      = link_to 'Login', login_path, :id => "login"
